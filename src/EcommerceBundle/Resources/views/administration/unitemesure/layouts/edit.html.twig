{% extends 'layout/layoutAdmin.html.twig' %}

{% block body %}
	<h3>Unites de mesure - Edit</h3>
	<form action="{{ path('unitemesureAdmin_edit', {'id': unitemesure.id }) }}" method="POST" {{ form_enctype(edit_form) }} >
	{{ form(edit_form) }}
	<br />
    {# <button class="btn btn-primary">Valider</button>  #}
    </form>    
	<ul class="record_actions">
		<li>
			<a href="{{ path('unitemesureAdmin_index') }}">
				Retour
			<a>
		</li>
		<li>
			{{form(delete_form)}}
		</li>
	</ul>
{% endblock %}
                        