{% extends 'layout/layoutAdmin.html.twig' %}

{% block body %}
	<h1>Liste des Commandes</h1>
	<table class="table table-striped table-hover">
		<thead>
			<tr>
				<th>Date commande</th>
				<th>Date livraison</th>
				<th>Référence</th>
				<th>Client</th>
				<th>Actions</th>
			</tr>
		<thead>
		<tbody>
			{% for entity in commandes %}
				{# {{ dump(entity) }} #}
				<tr>
					<td>{{ entity.datec|date('d/m/Y h:i:s') }}</td>
					<td>{{ entity.datel|date('d/m/Y h:i:s') }}</td>
					<td>{{ entity.reference }}</td>
					<td>{{ entity.creepar }}</td>
					
					<td>
						<ul>
							<li>
								{# <a href="{{ path('commandeAdmin_show', {'id':entity.id }) }}">
									<i class="icon-book"></i>
								<a> #}
							</li>
							<li>
								{# <a href="{{ path('commandeAdmin_edit', {'id':entity.id}) }}">
									<i class="icon-edit"></i>
								<a> #}
							</li>
						</ul>
					</td>
				</tr>
			{% endfor %}
			
			
		</tbody>
	</table>
	<ul class="record_actions">
		{# <li>
			<a href="{{ path('commandeAdmin_new') }}">
				Ajouter une Catégorie
			<a>
		</li> #}
	</ul>


{% endblock %}
